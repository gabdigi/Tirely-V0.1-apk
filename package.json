{
  "name": "tirely",
  "version": "1.0.0",
  "private": true,
  "description": "Coach budgétaire personnel IA",
  "scripts": {
    "build": "echo \"Application prête\"",
    "cap:sync": "npx cap sync",
    "cap:copy": "npx cap copy"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/android": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
